import * as React from 'react';
import { Waves } from '../../WaveListScreen';

interface WaveListProps {
  waves: Waves;
}

const WaveList = ({}: WaveListProps) => {
  return null;
};

export default WaveList;
